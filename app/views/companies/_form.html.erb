<%= form_with(model: company) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation" class="col-md-8 col-lg-6" style="    background: white;
    bottom: 10px;
    border-radius: 15px;
    box-shadow: 0px 0px 14px 0px #9e9e9e;
    text-align: center;
    font-family: SFProDisplay-Regular, Helvetica, Arial, sans-serif;
    font-size: 15px;
    color: red;
    z-index: 1;
    overflow: hidden;
    border: none;
    margin: 0 auto;
    width: 502px;">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
        <% company.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :manager %>
    <%= form.text_field :manager %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :terms %>
    <%= form.number_field :terms %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>



